version: '3.9'

services:
  test-postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: trainerhub_test
      POSTGRES_USER: trainerhub
      POSTGRES_PASSWORD: trainerhub
    ports:
      - '54329:5432'

  test-redis:
    image: redis:7-alpine
    ports:
      - '6389:6379'
