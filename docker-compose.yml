services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: .
      dockerfile: deploy/backend/Dockerfile
    restart: unless-stopped
    command: ["/app/deploy/backend/entrypoint.sh"]
    env_file: .env.example
    environment:
      DATABASE_URL: postgresql://trainerhub:trainerhub@postgres:5432/trainerhub
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    expose:
      - "8000"

  worker:
    build:
      context: .
      dockerfile: deploy/backend/Dockerfile
    restart: unless-stopped
    command: ["/app/deploy/backend/celery-worker.sh"]
    env_file: .env.example
    environment:
      DATABASE_URL: postgresql://trainerhub:trainerhub@postgres:5432/trainerhub
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

  beat:
    build:
      context: .
      dockerfile: deploy/backend/Dockerfile
    restart: unless-stopped
    command: ["/app/deploy/backend/celery-beat.sh"]
    env_file: .env.example
    environment:
      DATABASE_URL: postgresql://trainerhub:trainerhub@postgres:5432/trainerhub
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

  flower:
    build:
      context: .
      dockerfile: deploy/backend/Dockerfile
    restart: unless-stopped
    command: ["celery", "-A", "config.celery_app", "flower", "--port=5555"]
    env_file: .env.example
    environment:
      DATABASE_URL: postgresql://trainerhub:trainerhub@postgres:5432/trainerhub
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    expose:
      - "5555"

  frontend:
    build:
      context: .
      dockerfile: deploy/frontend/Dockerfile
    restart: unless-stopped
    env_file: .env.example
    environment:
      NEXT_PUBLIC_API_BASE_URL: /api/v1
      NEXT_PUBLIC_API_BASE: /api/v1
    depends_on:
      - backend
    expose:
      - "3000"

  nginx:
    image: nginx:1.27-alpine
    restart: unless-stopped
    depends_on:
      - backend
      - frontend
    ports:
      - "8080:80"
    volumes:
      - ./deploy/nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro

volumes:
  postgres_data:
  redis_data:
