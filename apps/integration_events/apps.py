from django.apps import AppConfig


class IntegrationEventsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.integration_events"
    verbose_name = "Integration Events"
